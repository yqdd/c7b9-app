<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.PianoApp"
            android:usesCleartextTraffic="true"
            tools:targetApi="31">
        <activity
                android:name=".activity.main.MainActivity"
                android:windowSoftInputMode="adjustResize"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ToolSelectionActivity" android:launchMode="singleTask"/>
        <activity android:name=".activity.piano.PianoToolActivity" android:launchMode="singleTask" android:screenOrientation="landscape"/>
        <activity android:name=".activity.metronome.MetronomeActivity" android:launchMode="singleTask"/>
        <activity android:name=".activity.chord.ChordComposeActivity" android:launchMode="singleTask"/>
        <activity android:name=".activity.chord.ChordSettingActivity" android:launchMode="singleTask"/>
        <activity android:name=".activity.rhythm.RhythmActivity" android:launchMode="singleTask"/>
        <activity android:name=".activity.rhythm.RhythmSettingActivity" android:launchMode="singleTask"/>
        <activity android:name=".activity.practise.PractiseActivity" android:launchMode="singleTask"/>
        <activity android:name=".activity.leaderboard.LeaderboardActivity" android:launchMode="singleTask"/>
        <activity android:name=".LoginActivity" android:launchMode="singleTask"/>
        <activity android:name=".RegistryActivity" android:launchMode="singleTask"/>
        <activity android:name=".AccountOptionsActivity" android:launchMode="singleTask"/>
        <!-- <activity android:name=".ChangeUsernameActivity" android:launchMode="singleTask"/> -->
    </application>
</manifest>