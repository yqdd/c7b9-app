<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PianoApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black_background</item>
        <item name="colorAccent">@color/white</item>
        <item name="android:textColorHighlight">@color/translucent_white</item>

        <item name="android:navigationBarColor">@color/black_background</item>
        <item name="android:windowBackground">@color/black_background</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>
</resources>